apiVersion: v1
kind: Service
metadata:
    name: poultonderazurevotingappredis
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: poultonderazurevotingappredis